name: ecommerce_app
description: An e-commerce app UI with mock data

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  device_preview: ^1.1.0
  flutter_riverpod: ^2.4.9
  fl_chart: ^0.61.0
  intl: ^0.19.0
  google_fonts: ^6.1.0
  cached_network_image: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/